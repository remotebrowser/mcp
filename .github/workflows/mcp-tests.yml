name: MCP Tests

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  mcp-pytest:
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Download and extract latest Chrome Fleet release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          url=$(gh api repos/remotebrowser/chromefleet/releases/latest \
            --jq '.assets[] | select(.name | endswith("linux-64bit.zip")) | .browser_download_url')
          curl -fsSL -o release.zip "$url"
          unzip release.zip -d app
          chmod +x app/chromefleet/chromefleet

      - name: Run Chrome Fleet
        run: app/chromefleet/chromefleet 2>&1 | tee chromefleet.log &

      - name: Wait until Chrome Fleet is ready
        run: while ! curl -s 'http://localhost:8300/health' | grep 'OK'; do sleep 1; done
        timeout-minutes: 3

      - name: Prepare back-end environment
        uses: ./.github/actions/prepare-backend

      - name: Create a test Chromium container
        run: curl -sf -X POST localhost:8300/api/v1/browsers/test123 | tee /dev/stderr | grep -q 'status'

      - name: Build MCP container
        run: |
          docker container rm -f getgather 2>/dev/null || true
          docker build --network=host -t getgather .

      - name: Run MCP container
        run: |
          docker run --network host --name getgather -d \
            -e CHROMEFLEET_URL="http://localhost:8300" \
            getgather
        timeout-minutes: 3

      - name: Run the health check validation
        run: while ! curl -s 'http://localhost:23456/health' | grep 'OK'; do sleep 1; done
        timeout-minutes: 3

      - name: Run the extended health check validation
        run: while ! curl -s 'http://localhost:23456/extended-health' | grep 'OK'; do sleep 1; done
        timeout-minutes: 3

      - name: Run MCP tests
        run: uv run pytest -m "mcp" -s -p no:xdist
        env:
          HOST: http://localhost:23456
          BBC_EMAIL: ${{ secrets.BBC_EMAIL }}
          BBC_PASSWORD: ${{ secrets.BBC_PASSWORD }}
          GOODREADS_EMAIL: ${{ secrets.GOODREADS_EMAIL }}
          GOODREADS_PASSWORD: ${{ secrets.GOODREADS_PASSWORD }}
          WAYFAIR_EMAIL: ${{ secrets.WAYFAIR_EMAIL }}
          WAYFAIR_PASSWORD: ${{ secrets.WAYFAIR_PASSWORD }}

      - name: Dump getgather container logs
        if: always()
        run: docker logs getgather || true
